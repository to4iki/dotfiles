prefix_rule(
    pattern = ["git", ["log", "status", "diff"]],
    decision = "allow",
    justification = "Allow git read-only commands."
)

prefix_rule(
    pattern = ["gh", "pr", "view"],
    decision = "allow",
    justification = "Allow gh pr read-only commands."
)
